from journal_rss.db import get_session
from journal_rss.models import Journal

# def store_journals(journals: dict):
#     """
#     Store journal data in DB from crossref query
#     """
#     with get_session()() as session:
#         for
